"""StyleSignal backend — descriptive style/outfit feedback.

Implements the v1 scope of ``stylesignal-spec.md``. Section references in
docstrings (§4.3, §7.5, ...) point at that document.
"""
__version__ = "1.0.0"
